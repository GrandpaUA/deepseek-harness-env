# @deepseek-ai packages must stay single-instance in the dsh process.
# They resolve upward via the profiles/node_modules junction layer into
# runtime/node_modules. Auto-installed peer copies create a second module
# instance and crash the tool scheduler ("reading 'prepare'").
auto-install-peers=false
